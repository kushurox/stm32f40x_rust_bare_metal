# Rust bare metal project targetting stm32f401fccu6 (blackpill board)

## project workflow idea
    - Compile the firmware code as a static lib
    - run linkerscript on it to produce final firmware binary